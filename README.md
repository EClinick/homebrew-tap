# EClinick Homebrew Tap

Homebrew casks for [Juice](https://github.com/EClinick/juice).

```bash
brew tap EClinick/tap
brew install --cask EClinick/tap/juice
```

The initial release is a development build. A Developer ID-signed and
notarized release will replace it once the signing certificate is configured.
